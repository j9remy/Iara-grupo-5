import { BrowserRouter, Route, Routes } from "react-router-dom";

function Rotas(){
    return(
        <BrowserRouter>
            <Routes>
                
            </Routes>
        </BrowserRouter>

    );
}

export default Rotas;